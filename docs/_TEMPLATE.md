# <Feature name>

> One-line description of what this feature does for the user.

## Overview

What the feature is and how it behaves from the user's point of view.

## Key files

| File | Responsibility |
| --- | --- |
| `src/renderer/<file>.js` | ... |
| `src/main/<file>.js` | ... |
| `src/styles/<file>.css` | ... |

## How it works

Main flow, and the main ↔ renderer IPC contract (message/channel names, payload
shapes). Note any invariants a future agent must preserve.

## Gotchas

- Non-obvious behavior, edge cases, platform quirks, ordering requirements, etc.

## Change log

Newest first. Each entry: date, who/what, and the change.

- **YYYY-MM-DD** — Initial doc created.
